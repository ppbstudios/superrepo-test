// const products = new Products(null);

const products:[] = [];
const collections:[] = [];
const franchises:[] = [];

const model = {
  cart: {},
  products,
  collections,
  franchises,
};

export default model;
